# Supabase Auth — local development
# Populated automatically by: npm run configure:supabase
# Dashboard: Project Settings → API

# Required
VITE_SUPABASE_URL=

# Use either name (Publishable Key is preferred; same value as legacy anon key):
VITE_SUPABASE_PUBLISHABLE_KEY=
# VITE_SUPABASE_ANON_KEY=
